// lib/screens/pickup_tab.dart
import 'package:flutter/material.dart';

class PickupTab extends StatelessWidget {
  const PickupTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Pickup System Coming Soon!',
        style: TextStyle(fontSize: 18, fontFamily: 'Orbitron', color: Colors.tealAccent),
      ),
    );
  }
}